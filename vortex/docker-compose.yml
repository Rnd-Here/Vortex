version: '3.8'

services:
  vortex-api:
    build: ./backend-py
    container_name: vortex-api
    ports:
      - "8002:8002"
    volumes:
      - ./backend_data:/app/data
    environment:
      - SPRING_DATASOURCE_URL=sqlite:///app/data/chat_app.db
      - LITELLM_PROXY_BASE_URL=${LITELLM_PROXY_BASE_URL}
      - SECRET_KEY=${SECRET_KEY}
      - CHROMA_URL=http://chromadb:8000
      - CHROMA_COLLECTION_NAME=${CHROMA_COLLECTION_NAME:-org_knowledge}
    restart: unless-stopped

  vortex-frontend:
    build: ./frontend
    container_name: vortex-frontend
    ports:
      - "80:80"
    depends_on:
      - vortex-api
    restart: unless-stopped

  chromadb:
    image: chromadb/chroma:latest
    container_name: vortex-chroma
    ports:
      - "8001:8000"
    volumes:
      - ./chroma_data:/chroma/chroma
    restart: unless-stopped

volumes:
  backend_data:
  chroma_data: